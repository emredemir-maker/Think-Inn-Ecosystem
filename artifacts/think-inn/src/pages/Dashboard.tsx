import DashboardPage from "./DashboardPage";

/* Dashboard / — Hub UI Kit executive layout.
 * Welcome row + 4 KPI tile + Pipeline + AI Panel + Lower row.
 * Gerçek DB verisiyle çalışır (mock yok).
 *
 * Not: Eski VitrinePanel (4 sekmeli vitrin) /research, /projects, /map'e
 * ayrıldı. Burada sadece dashboard görüntüsü var.
 */
export default function Dashboard() {
  return <DashboardPage />;
}
